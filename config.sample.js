let path = require('path')
module.exports = function() {
  return {
    activities: [
      path.join(__dirname, '../activities')
      'my-npm-module',
      'another-npm-module'
    ],
    swf: {
      domain: 'ftl-engine-prod'
    },
    notififier: {
      snsTopic:

    },
    logger:
  }
}
